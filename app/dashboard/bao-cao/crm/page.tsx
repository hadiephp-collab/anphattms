import ComingSoon from '@/components/ComingSoon';
export default function BaoCaoCRMPage() {
  return <ComingSoon title="Báo Cáo CRM" description="Đối tác mới theo tháng, tỷ lệ quay lại (retention), tần suất mua hàng." />;
}
